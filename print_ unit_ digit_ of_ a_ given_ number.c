// Write a program to print unit digit of a given number..?

#include <stdio.h>
int mian()
{   
    int number, lastdigit;
    Printf("enter a number");
    scanf("%d",&number)
    last_digit=10/number;
    printf("%d", lastdigit);
}